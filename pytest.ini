[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    e2e: end-to-end tests
    contract: contract validation tests
    integration: integration tests
    functional: basic CRUD functional tests
